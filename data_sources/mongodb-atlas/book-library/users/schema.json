{
    "bsonType": "object",
    "properties": {
        "FavoriteBooks": {
            "bsonType": "array",
            "description": "The list of favorite books by the user",
            "items": {
                "bsonType": "objectId",
                "description": "The id of the book"
            }
        },
        "NumberOfBooksRead": {
            "bsonType": "int",
            "description": "The number of books read by the user"
        },
        "_id": {
            "bsonType": "objectId",
            "description": "The id of the user"
        },
        "createdAt": {
            "bsonType": "date",
            "description": "The date the user was created"
        },
        "email": {
            "bsonType": "string",
            "description": "The email of the user"
        },
        "password": {
            "bsonType": "string",
            "description": "The password of the user"
        },
        "reviews": {
            "bsonType": "array",
            "description": "The list of reviews written by the user",
            "items": {
                "bsonType": "objectId",
                "description": "The id of the review"
            }
        },
        "updatedAt": {
            "bsonType": "date",
            "description": "The date the user was updated"
        },
        "username": {
            "bsonType": "string",
            "description": "The name of the user"
        }
    },
    "required": [
        "_id",
        "name",
        "email",
        "password"
    ]
}
